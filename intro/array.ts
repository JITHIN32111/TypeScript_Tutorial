const superHeros:string[]=[]
const heroPower:Array<number>=[]

type UserOne={
    name:string
    isActive:boolean
}
const allUser:UserOne[]=[]
// this means allUser array should only contain [{name and isActive}]
// const allUser:Array<UserOne>=[]


superHeros.push("bat man")
allUser.push({name:"jith",isActive:false})